{
# Request body to be sent to Digitransit API
"request_body": "{
                    stops(ids: ["HSL:1130110", "HSL:1130438"]) {
                        code
                        lat
                        lon
                        stoptimesWithoutPatterns (startTime: START_TIME_PLACEHOLDER, numberOfDepartures: 15) {
                        realtimeDeparture
                        realtime
                        trip {
                            route {
                            shortName
                            mode
                            }
                        }
                        headsign
                        }
                    }
                }",

# Offset for start time in seconds:
# only departures after this time from now will be requested from Digitransit API
"start_offset": 180,

# Maximum amount of departure rows to show per div on the page
"max_departures_to_show": 18
}
